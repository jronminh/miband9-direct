# Contributors

- **jronminh** — author and maintainer.
- **opencode** (`deepseek-v4-flash`) — AI pair programmer: Xiaomi protocol
  client, the `mibandd` backend/CLI, the activity decoder and the test suite.

This project is licensed **AGPL-3.0-or-later** (see `LICENSE`). The protocol
client (`client/protocol/`, `client/activity.py`) and `store/summary.py` are
Python translations of [Gadgetbridge](https://gadgetbridge.org) (AGPL-3.0) and
carry SPDX + attribution headers; see `contrib/` and `docs/`.
