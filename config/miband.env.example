# Copy to config/miband.env (gitignored) or export these in the environment.
# `install.sh` auto-detects the MAC via dsh if it is unset here.

# Band Bluetooth MAC (e.g. AA:BB:CC:DD:EE:FF).
MIBAND_MAC=

# Daemon TCP port (default 8477).
MIBAND_PORT=8477
